export type overwiewParagraph = {
  text: string;
  id: string;
};

export type feedback = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNum: string;
  message: string;
  selectedCountry: string;
};

export type image = {
  src: string;
  alt: string;
};
